{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build:vercel",
  "outputDirectory": "client/dist",
  "//installCommand": [
    "Shared is built during install, not build, so its dist/ exists before",
    "BOTH the client build and the serverless function bundling — the",
    "function resolves @tms/shared through node_modules and must find",
    "compiled JS there, whatever order Vercel runs its steps in."
  ],
  "installCommand": "npm install && npm -w shared run build",
  "framework": null,
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
