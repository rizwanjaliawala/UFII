{
  "name": "utopia-tms",
  "version": "1.0.0",
  "private": true,
  "description": "Utopia Fulfillment Inc — Transportation Management System",
  "workspaces": ["shared", "server", "client"],
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently -n api,web -c green,cyan \"npm -w server run dev\" \"npm -w client run dev\"",
    "build": "npm -w shared run build && npm -w server run build && npm -w client run build",
    "build:vercel": "npm -w shared run build && npm -w client run build",
    "migrate": "npm -w server run migrate",
    "test": "npm -w shared test",
    "typecheck": "npm -w shared run typecheck && npm -w server run typecheck && npm -w client run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
